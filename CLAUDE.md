# CLAUDE.md（着物花柄認識アプリ）

> Claude Code に向けたプロジェクト概要・実装指示書です。

## 作りたいもの

着物の写真を撮影・アップロードすると、描かれている**花の種類を判定**して、名前や意味・季節などを教えてくれる画像認識アプリ。

## 解決したい課題

- 誰が：着物を着る/見る機会はあるが、柄の花の種類や意味に詳しくない人（着付け初心者、観光客、着物好きなど）
- どんな場面で：着物や帯を見て「これは何の花だろう」「季節に合っているのかな」と気になった時
- 何に困っているか：花の種類（桜・梅・牡丹・菊・藤・椿など）は見た目が似ているものも多く、詳しい人に聞かないとわからない。図鑑的に調べようにも、着物の柄はデフォルメされていて一般的な植物図鑑とは照合しづらい

## 使いたい技術（無料・ライセンスクリア前提）

> 画像認識は、ゼロから独自モデルを学習させると学習データの収集・著作権処理が大変なため、**マルチモーダルLLM（Claude APIの画像入力機能）に判定させる方式**を推奨します。既存の花の画像を学習データとして収集・保持する必要がなく、権利的なリスクを避けやすいのが利点です。

- フロントエンド：React（Web版、カメラ入力に対応。将来的にReact Nativeでアプリ化も検討）
- 画像認識：Claude API（画像入力対応モデル）に「この着物の柄に描かれている花は何か」を判定させるプロンプトを設計
- ホスティング：Vercel 無料枠 または GitHub Pages
- データ保存：判定結果の履歴を残す場合はSupabase（無料枠）、残さないならバックエンドDB不要でAPI呼び出しのみでも成立

> 注意：Claude APIは無料枠ではなく従量課金制です。個人開発・ポートフォリオ用途であれば呼び出し回数は少なく、コストはごく小額で収まる想定ですが、無制限に無料というわけではない点は留意してください。

## 入力

- 着物・帯の写真（カメラ撮影 or 画像アップロード）

## 出力

- 判定された花の名前（複数の花が写っている場合は候補を複数表示）
- 花の意味・由来（例：菊＝長寿、椿＝魔除けなど）
- その花が象徴する季節（着物の柄は「季節を先取りする」という着こなしのルールがあるため、実際の季節との比較コメントがあると面白い）
- 判定の確信度（LLMの回答が曖昧な場合はその旨も表示し、誤情報として断定しないようにする）

## 実装の進め方（MVP方針）

1. 画像アップロード（またはカメラ撮影）→ Claude APIに送信 → テキストで結果を受け取る、という一連の流れを最小構成で実装
2. プロンプト設計：花の名前だけでなく「意味」「季節」もセットで返すよう指示し、JSON形式で構造化して返させる（表示側で扱いやすくするため）
3. 判定結果の表示UIを作り込む（花の名前・意味・季節をカード形式で表示など）
4. （後回しでOK）判定履歴の保存、複数候補のスコア表示、季節とのズレをコメントする機能

## プロンプト設計の注意点

- 「必ず1つの花に断定する」のではなく、**候補が複数ある場合はそれを正直に返す**よう指示する（着物の柄はデフォルメされ、実在しない花や複数の花が組み合わさっている場合もあるため）
- 判定不能な場合（花ではなく幾何学模様など）は「花柄ではない可能性があります」と返すよう指示する
- 出力はJSON形式に固定し、フロント側でパースしやすくする

## 未確定事項（要確認しながら進める）

- 判定結果の履歴を保存する機能を持たせるか（持たせる場合はログイン機能も必要になり、忍者修行ログと似た構成になる）
- Claude API呼び出しのコストが想定より高くなった場合の代替策（呼び出し回数制限、キャッシュなど）
- 花以外の柄（扇・波・雲など）にも対応を広げるか、まずは花柄に絞るか
